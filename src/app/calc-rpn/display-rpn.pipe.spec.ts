import { DisplayRPNPipe } from './display-rpn.pipe';

describe('DisplayRPNPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRPNPipe();
    expect(pipe).toBeTruthy();
  });
});
